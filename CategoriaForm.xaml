<Window x:Class="GestorDeInventario.CategoriaForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Categoría" Height="200" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Nombre de Categoría:"/>
        <TextBox x:Name="txtNombreCategoria" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Button x:Name="btnGuardar" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="Guardar" Margin="5" Click="btnGuardar_Click"/>
        <Button x:Name="btnCancelar" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Cancelar" Margin="5" Click="btnCancelar_Click"/>
    </Grid>
</Window>