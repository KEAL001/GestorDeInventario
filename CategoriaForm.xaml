<Window x:Class="GestorDeInventario.CategoriaForm"  
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
        Title="Gestión de Categoría"   
        Height="473" Width="574"   
        WindowStartupLocation="CenterScreen"  
        ResizeMode="NoResize"  
        Background="#F8F9FA">

    <Window.Resources>
        <!-- Estilos Modernos -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"   
                                CornerRadius="8"  
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000"   
                                                Direction="270"   
                                                ShadowDepth="2"   
                                                BlurRadius="8"   
                                                Opacity="0.2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"   
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#757575"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" CornerRadius="0,0,15,15">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#2196F3" Offset="0"/>
                    <GradientStop Color="#1976D2" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="3" BlurRadius="10" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="GESTIÓN DE CATEGORÍA"   
                         FontSize="20" FontWeight="Bold" Foreground="White"   
                         HorizontalAlignment="Center"/>
                <TextBlock Text="Crear o editar categoría de productos"   
                         FontSize="12" Foreground="#E3F2FD"   
                         HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Formulario Principal -->
        <Border Grid.Row="1" Background="White" Margin="20,5,20,72" CornerRadius="10" Padding="30" Grid.RowSpan="2">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.1"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <!-- Campo Nombre de Categoría -->
                <TextBlock Text="Nombre de Categoría:" FontWeight="SemiBold"   
                         Foreground="#424242" Margin="0,0,0,8" FontSize="14"/>

                <Border BorderBrush="#E0E0E0" BorderThickness="2"   
                        CornerRadius="8" Margin="0,0,0,20">
                    <TextBox x:Name="txtNombreCategoria"   
                           Padding="15,12" FontSize="14"  
                           BorderThickness="0" Background="Transparent"  
                           FontFamily="Segoe UI"/>
                </Border>

                <!-- Información Adicional -->
                <Border Background="#E3F2FD" CornerRadius="8" Padding="15,10" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💡" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="El nombre de la categoría se convertirá automáticamente a mayúsculas."   
                                 FontSize="12" Foreground="#1976D2" TextWrapping="Wrap" Height="30" Width="365"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Botones de Acción -->
        <Border Grid.Row="2" Background="White" Padding="20,15" Margin="20,12,20,8" CornerRadius="10">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="1" BlurRadius="5" Opacity="0.1"/>
            </Border.Effect>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnGuardar"   
                        Style="{StaticResource ModernButtonStyle}"  
                        Content="GUARDAR"   
                        Width="120"  
                        Click="btnGuardar_Click"/>

                <Button x:Name="btnCancelar"   
                        Style="{StaticResource CancelButtonStyle}"  
                        Content="CANCELAR"   
                        Width="120"  
                        Click="btnCancelar_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>